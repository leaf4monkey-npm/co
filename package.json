{
  "name": "async-hooked-co",
  "version": "0.0.2",
  "description": "generator async control flow goodness, with `async_hooks` support.",
  "keywords": [
    "async",
    "flow",
    "generator",
    "coro",
    "coroutine"
  ],
  "devDependencies": {
    "browserify": "^10.0.0",
    "chai": "^4.2.0",
    "co": "^4.6.0",
    "istanbul-harmony": "0",
    "mocha": "^2.0.0",
    "mz": "^1.0.2"
  },
  "scripts": {
    "test": "mocha",
    "test-cov": "node node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha -- --reporter dot",
    "test-travis": "node node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report lcovonly -- --reporter dot",
    "prepublishOnly": "yarn test"
  },
  "files": [
    "index.js"
  ],
  "license": "MIT",
  "repository": "tj/co",
  "engines": {
    "node": ">= 8.12.0"
  }
}
